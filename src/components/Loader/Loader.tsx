import { LoaderContainer, LoadingSpinner } from './loader-styles';

const Loader = () => {
  return (
    <LoaderContainer>
      <LoadingSpinner />
    </LoaderContainer>
  );
};

export default Loader;
